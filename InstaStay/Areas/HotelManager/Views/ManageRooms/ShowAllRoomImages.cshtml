@model IEnumerable<Models.Models.RoomImages>

@{
    ViewData["Title"] = "Room Images";
}
@section scripts{
    @if (@TempData["success"] != null)
    {
        <script>
            toastr.success('@TempData["success"]')
        </script>
    }
}
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="text-primary"><i class="fas fa-images"></i> Room Images</h2>
        <a href="/HotelManager/ManageRooms/AddRoomImage/@ViewBag.Id" class="btn btn-success">
            <i class="fas fa-plus"></i> Add New Image
        </a>
    </div>

    <div class="table-responsive">
        <table class="table table-hover table-bordered shadow">
            <thead class="table-dark text-center">
                <tr>
                    <th>Image</th>
                    <th>Room Type</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody class="text-center">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <img src="~/images/RoomImages/@item.Image" alt="Room Image" class="img-thumbnail" style="width: 100px; height: 100px; object-fit: cover;">
                        </td>
                        <td>@item.Room.RoomType</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm">
                                <i class="fas fa-edit"></i> Edit
                            </a>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info btn-sm">
                                <i class="fas fa-info-circle"></i> Details
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this image?');">
                                <i class="fas fa-trash-alt"></i> Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <a href="/HotelManager/ManageRooms/Edit/@ViewBag.Id" class="btn btn-secondary mt-3"><i class="fas fa-arrow-left"></i> Back to List</a>
</div>